<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ image['title'] }} - Image Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
    <h2>{{ image['title'] }}</h2>
    <img src="{{ url_for('serve_image', filename=image['image_path'].split('/')[-1]) }}" class="img-fluid mb-4" alt="{{ image['title'] }}">
    <p><strong>Description:</strong> {{ image['description'] }}</p>
    <p><strong>Tags:</strong> {{ ', '.join(image['tags']) }}</p>
    <a href="{{ url_for('get_catalog') }}" class="btn btn-secondary">Back to Catalog</a>
</div>
<!-- Bootstrap JS -->
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
